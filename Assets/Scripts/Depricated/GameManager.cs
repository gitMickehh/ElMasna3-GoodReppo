using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour {

    //public List<AssemblyLine_New> assemblyLines;
    //public bool workerAdded = false;
    public Worker newWorker;

    /*public void AddWorkerToAssembly()
    {
        for(int i= 0; i<assemblyLines.Count; i++)
        {
            if (!assemblyLines[i].full)
            {
                assemblyLines[i].AddWorkerToLine(newWorker);
                return;
            }
        }
    }*/

}
